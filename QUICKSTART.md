# Хурдан эхлэх заавар (Quick Start Guide)

## 1. Docker ашиглан 5 минутад эхлүүлэх

### Шаардлагатай
- Docker Desktop (Windows/Mac) эсвэл Docker Engine (Linux)

### Алхам 1: Repository татаж авах
```bash
git clone <repository-url>
cd medee
```

### Алхам 2: Ажиллуулах
```bash
docker-compose up -d
```

### Алхам 3: Хандах
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8080/api
- **MongoDB**: mongodb://localhost:27017

### Алхам 4: Анхдагч хэрэглэгчүүд

Системд бүртгүүлэх хуудаснаас дараах хэрэглэгчдийг үүсгэж болно:

#### Admin хэрэглэгч үүсгэх:
- Username: `admin`
- Email: `admin@medee.mn`
- Password: `admin123`
- Role: `ADMIN` (бүртгэлийн үед сонгох)

#### Author хэрэглэгч үүсгэх:
- Username: `author1`
- Email: `author1@medee.mn`
- Password: `author123`
- Role: `AUTHOR`

#### Reader хэрэглэгч үүсгэх:
- Username: `reader1`
- Email: `reader1@medee.mn`
- Password: `reader123`
- Role: `READER`

## 2. Системийг туршиж үзэх

### Reader (Уншигч) дүрээр:
1. Нүүр хуудас руу орох
2. Нийтлэгдсэн мэдээнүүдийг унших
3. Мэдээнд таалагдах дарах
4. Сэтгэгдэл үлдээх

### Author (Нийтлэгч) дүрээр:
1. Author хэрэглэгчээр нэвтрэх
2. "Миний мэдээ" хэсэгт орох
3. "Шинэ мэдээ" дарж мэдээ бичих
4. Draft.js editor ашиглан:
   - Bold, Italic, код блок нэмэх
   - Code snippet оруулах
   - Гарчиг, жагсаалт нэмэх
5. "Шалгалтанд илгээх" дарах
6. Миний мэдээний жагсаалтаас статусыг харах

### Admin (Админ) дүрээр:
1. Admin хэрэглэгчээр нэвтрэх
2. "Удирдлага" → "Шалгалт хүлээж байгаа мэдээ" орох
3. Мэдээг харах, унших
4. Зөвшөөрөх эсвэл буцаах
5. Цалингийн удирдлагад орж:
   - Сар сонгох
   - "Цалин тооцоолох" дарах
   - Тайлан харах

## 3. Систем зогсоох

```bash
docker-compose down
```

## 4. Өгөгдөл цэвэрлэх

```bash
docker-compose down -v
```

## 5. Локал хөгжүүлэлт

### Backend (Java/Spring Boot):
```bash
cd backend
./gradlew bootRun
```

### Frontend (React/Vite):
```bash
cd frontend
npm install
npm run dev
```

### MongoDB:
```bash
# Docker ашиглах
docker run -d -p 27017:27017 --name mongodb mongo:7.0

# Эсвэл локал MongoDB ажиллуулах
mongod --dbpath=/path/to/data
```

## 6. Асуудал шийдвэрлэх

### Backend ажиллахгүй байвал:
1. MongoDB ажиллаж байгаа эсэхийг шалгах: `docker ps | grep mongodb`
2. Log-ийг харах: `docker logs medee-backend`
3. Port 8080 чөлөөтэй эсэхийг шалгах

### Frontend ажиллахгүй байвал:
1. Backend ажиллаж байгаа эсэхийг шалгах
2. Log-ийг харах: `docker logs medee-frontend`
3. Port 3000 чөлөөтэй эсэхийг шалгах

### MongoDB холбогдохгүй байвал:
1. MongoDB container ажиллаж байгаа эсэхийг шалгах: `docker ps`
2. MongoDB log харах: `docker logs medee-mongodb`

## 7. Хувилбар шинэчлэх

```bash
# Шинэ код татаж авах
git pull

# Container-үүдийг дахин build хийж ажиллуулах
docker-compose up -d --build
```

## 8. Production Deploy

### AWS, Azure, GCP дээр:
1. MongoDB Atlas үүсгэх
2. Docker images бүтээх
3. Container registry-д push хийх (ECR, ACR, GCR)
4. Kubernetes эсвэл Cloud Run дээр deploy хийх

Дэлгэрэнгүй заавар: [README.md](README.md)

---

**Амжилт хүсье! 🚀**

